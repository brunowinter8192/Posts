export interface SiteConfig {
	author: string;
	description: string;
	lang: string;
	ogLocale: string;
	showLogo: boolean;
	title: string;
	url: string;
}

export interface SiteMeta {
	description?: string;
	ogImage?: string;
	title: string;
}
